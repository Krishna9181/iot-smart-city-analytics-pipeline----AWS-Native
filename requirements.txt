# Smart City IoT Analytics - Python Dependencies

# AWS SDK
boto3==1.34.90
botocore==1.34.90

# Data Processing (for Lambda functions)
pandas==2.2.2
numpy==1.26.4

# JSON Processing
python-json-logger==2.0.7

# Date/Time Handling
python-dateutil==2.9.0
pytz==2024.1

# Environment Variables
python-dotenv==1.0.1

# AWS Lambda Powertools (optional, for production)
aws-lambda-powertools==2.37.0

# Testing (optional)
pytest==8.1.1
moto==5.0.5  # AWS service mocking

# PySpark (for local Glue development/testing)
# Note: In production, Glue provides PySpark runtime
pyspark==3.5.0

# Data Validation
great-expectations==0.18.12  # Optional: for data quality checks

# Logging
structlog==24.1.0

# HTTP Requests (for API integrations, if needed)
requests==2.31.0

# Note: When deploying Lambda functions, create deployment packages:
# pip install -r requirements.txt -t ./package
# cd package && zip -r ../lambda-deployment.zip . && cd ..
# zip -g lambda-deployment.zip lambda_function.py
